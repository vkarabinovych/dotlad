#!/usr/bin/env bash
# Thin executable entrypoint; CLI implementation lives under lib/cli/.
set -euo pipefail

DOTLAD_RUNTIME_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

# shellcheck source=lib/cli/main.sh
. "$DOTLAD_RUNTIME_ROOT/lib/cli/main.sh"

cli_main "$@"
